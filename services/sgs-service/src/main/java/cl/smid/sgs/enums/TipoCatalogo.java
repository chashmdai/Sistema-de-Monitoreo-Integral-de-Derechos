package cl.smid.sgs.enums;

/** Discriminador de la tabla de catálogos parametrizables (decisión #9). */
public enum TipoCatalogo {
    MATERIA,
    CATEGORIA,
    TIPO_SEGUIMIENTO,
    TIPO_RESPUESTA
}
